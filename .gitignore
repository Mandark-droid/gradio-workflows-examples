# Local environment — never in a public repo
.venv/
venv/
env/
.env
.env.*
!.env.example

# IDE / editor
.idea/
.vscode/
*.swp
.DS_Store

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
build/
dist/

# Run artifacts — may contain model outputs and are regenerable
runs/
*.ckpt.jsonl
recordings/
**/eval/results/

# Secret-scanner deny-list — holds internal names, must never be public
.secretscan-deny

# Controller scratch: plan ledgers and task reports. Never part of the repo.
.superpowers/

# Implementation plans are working artifacts: long, superseded by the code
# they produced, and kept locally. The design specs under docs/superpowers/
# specs/ ARE tracked — they are the binding record of what was built and why.
docs/superpowers/plans/
